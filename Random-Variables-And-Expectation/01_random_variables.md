# Random Variables

A **random variable (r.v.)** is a measurable function:
```
X : (Ω, 𝔽) → (ℝ, ℬ(ℝ))
```
where ℬ(ℝ) is the Borel σ-algebra.

## 1. Why a function?
A random variable maps each outcome to a number, enabling probability → integrals → expectations.

## 2. Types
- Discrete
- Continuous
- Mixed

## 3. Distribution
```
P_X(A) = P(X ∈ A)
```
Represented by CDF, PMF, PDF, MGF, characteristic function.

## 4. Examples
### Dice
P(X=k)=1/6.
### Uniform(0,1)
Density=1 on [0,1].
### Brownian increment
Normal(0,Δ).
